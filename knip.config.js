const config = {
    ignore: [],
    ignoreBinaries: ['convox', 'mkcert', 'eslint'],
    ignoreDependencies: [],
    paths: {},
    rules: {
        enumMembers: 'off',
        unlisted: 'off',
    },
};

export default config;
