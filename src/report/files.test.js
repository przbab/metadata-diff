'use strict';

describe('report', () => {
    describe('files', () => {
        describe('getScripts', () => {
            test.todo(`should read default file`);
            test.todo(`should read user specified file`);
            test.todo(`should minify file`);
            test.todo(`should throw error on bad read`);
        });
        describe('getStyles', () => {
            test.todo(`should read default file`);
            test.todo(`should read user specified file`);
            test.todo(`should minify file`);
            test.todo(`should throw error on bad read`);
        });
        describe('getHtml', () => {
            test.todo(`should read default file`);
            test.todo(`should read user specified file`);
            test.todo(`should inject data, scripts and styles to the file`);
            test.todo(`should minify file`);
            test.todo(`should throw error on bad read`);
        });
    });
});
